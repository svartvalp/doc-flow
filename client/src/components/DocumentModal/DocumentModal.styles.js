import { makeStyles } from "@material-ui/core/styles";
export const useStyles = makeStyles((theme) => ({
  root: {
    position: "absolute",
    left: "25%",
    top: "10%",
    width: "50vw",
    height: "70vh",
    zIndex: 600,
  },
}));
